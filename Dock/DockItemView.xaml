<UserControl x:Class="TaskbarDock.Dock.DockItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:TaskbarDock.UI.Converters"
             mc:Ignorable="d" 
             Cursor="Hand"
             MouseLeftButtonUp="OnMouseLeftButtonUp"
             ToolTipService.InitialShowDelay="300"
             ToolTipService.BetweenShowDelay="100">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:BoolToOpacityConverter x:Key="BoolToOpacity"/>

        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#E01E1E1E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.4"/>
                            </Border.Effect>
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ToolTipService.ToolTip>
        <ToolTip Content="{Binding DisplayName}"/>
    </ToolTipService.ToolTip>

    <UserControl.ContextMenu>
        <ContextMenu Background="#F0202020" Foreground="#FFFFFF" BorderBrush="#40FFFFFF" BorderThickness="1">
            <ContextMenu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="MenuItem">
                                <Border x:Name="Border" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#30FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ContextMenu.Resources>
            <MenuItem Header="Open" Click="OnMenuOpen"/>
            <MenuItem Header="Open in New Window" Click="OnMenuOpenNew"/>
            <Separator Background="#30FFFFFF"/>
            <MenuItem Header="Remove from Dock" Click="OnMenuRemove"/>
        </ContextMenu>
    </UserControl.ContextMenu>

    <Grid x:Name="RootGrid" VerticalAlignment="Bottom" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon container with smooth bounce animation -->
        <Grid x:Name="IconContainer" Grid.Row="0" Width="{Binding CurrentSize}" Height="{Binding CurrentSize}"
              RenderTransformOrigin="0.5,1.0">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="BounceScale" ScaleX="1.0" ScaleY="1.0"/>
                    <TranslateTransform x:Name="BounceTranslate" Y="0"/>
                </TransformGroup>
            </Grid.RenderTransform>

            <Image Source="{Binding IconImage}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" Margin="2">
                <Image.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270" Opacity="0.35"/>
                </Image.Effect>
            </Image>
        </Grid>

        <!-- macOS Running Indicator Dot -->
        <Ellipse Grid.Row="1" Width="4" Height="4" Margin="0,3,0,1"
                 Fill="#FFFFFF"
                 HorizontalAlignment="Center"
                 Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}">
            <Ellipse.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="0" Color="#FFFFFF" Opacity="0.8"/>
            </Ellipse.Effect>
        </Ellipse>
    </Grid>
</UserControl>
